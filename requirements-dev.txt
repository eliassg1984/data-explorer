# Herramientas de desarrollo. NO se instalan en Streamlit Cloud — ese lee
# requirements.txt, y meter un linter ahí solo alarga el build de cada deploy.
#
#     pip install -r requirements-dev.txt
#
# Antes de pushear:
#     python -m ruff check .      (config en ruff.toml)
#     python test_graficos.py     (smoke de figuras + contratos del dispatcher)

ruff>=0.16,<0.17

# Exportar figuras Plotly a PNG sin navegador (herramientas/ver_figura.py).
# La primera vez hay que bajarle su Chrome, una sola vez por máquina:
#     python -c "import kaleido; kaleido.get_chrome_sync()"
kaleido>=1.0,<2

# Bajar PDF/XML originales del portal SOL (herramientas/sunat_originales_sync.py).
# Corre SOLO local — un Chromium real no cabe en Streamlit Cloud. La primera
# vez hay que instalarle el navegador, una sola vez por máquina:
#     playwright install chromium
playwright>=1.47,<2
